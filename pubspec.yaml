name: device_user_agent
description: Flutter plugin to build user agent containing device information.
version: 0.1.3
repository: https://github.com/AcmeSoftwareLLC/device_user_agent
homepage: https://acmesoftware.com

environment:
  sdk: ">=3.9.2 <4.0.0"
  # flutter: ">=3.0.0"

dependencies:
  device_info_plus: ^11.1.1 ## active (fluttercommunity.dev)
  flutter:
    sdk: flutter
  package_info_plus: ^8.0.2 ## active (fluttercommunity.dev)
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
